# Strictly Dancing Backend

FastAPI backend for the Strictly Dancing dance host marketplace.

## Development

```bash
# Install dependencies
uv sync

# Run development server
uv run uvicorn app.main:app --reload --port 8001

# Run tests
uv run pytest
```
